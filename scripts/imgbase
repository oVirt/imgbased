#!/bin/bash

[[ -d "src/imgbased" ]] && { echo "Enabling in-tree mode" ; export PYTHONPATH="$PWD/src" ;}

PYTHON=python

if [[ -n "$(which python3)" ]]; then
  PYTHON=python3
fi

$PYTHON -m imgbased.__main__ $@
